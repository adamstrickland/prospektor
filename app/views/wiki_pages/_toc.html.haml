.toc
  %h2 Table of Contents
  %ul
    %li=link_to 'Main', wiki_pages_url
    %li=link_to 'FAQ', wiki_page_path('FAQ')
    -if @pages.present?
      -wiki_paginate @pages do
        -@pages.each do |page|
          %li=link_to page.title, wiki_page_path(page) unless page.path == 'FAQ'